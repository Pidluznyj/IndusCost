import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient(); async function main() { console.log("Deletando mapeamentos DRE do tipo SEED..."); const result = await prisma.financialDreCostCenterMapping.deleteMany({ where: { source: "SEED" } }); console.log(`Deletados ${result.count} registros.`); } main().catch(console.error).finally(() => prisma.$disconnect());
